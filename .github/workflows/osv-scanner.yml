name: OSV-Scanner

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "37 04 * * 1-5" # "At 04:37 on every day-of-week from Monday through Friday."

# Declare default permissions as empty to follow the principle of least privilege.
permissions: {}

jobs:
  scan:
    name: Vulnerability Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    steps:
      - name: "Checkout"
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: "Run OSV-Scanner"
        uses: google/osv-scanner-action/osv-scanner-action@e80ced6cfb1e2b2a6a1e4711c08e3b5e9e004776 # v2.0.2
        with:
          scan-args: |-
            --recursive
            ./
